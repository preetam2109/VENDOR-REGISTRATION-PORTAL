export class PipelineDDLTransit {
    ponoid: number;
    details: string;

    constructor(ponoid: number,
        details: string) {
        this.ponoid = ponoid;
        this.details = details;
    }
}